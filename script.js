// Minimal script for now, can be expanded for more interactivity
document.addEventListener('DOMContentLoaded', () => {
    console.log("Portfolio Loaded");
    
    // Optional: Add more complex JS animations here if needed
    // For example, a typing effect or cursor follower
});
